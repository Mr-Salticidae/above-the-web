{
  "name": "above-the-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "蛛网之上 · 跳蛛先生创作知识库的杂志风开源展示站",
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro build && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.6.1",
    "unist-util-visit": "^5.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
